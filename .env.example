# Database Configuration
# For MySQL (default for Docker)
DATABASE_URL="mysql://ticketer:ticketerpassword@localhost:3306/ticketer"

# For SQLite (alternative for local development - requires changing prisma/schema.prisma provider to "sqlite")
# DATABASE_URL="file:./prisma/dev.db"

# Next.js Configuration
NEXT_TELEMETRY_DISABLED=1
